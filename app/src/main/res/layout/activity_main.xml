<?xml version="1.0" encoding="utf-8"?>
<!--TODO (6) Remove the line that declares the id, we don't need it-->
<FrameLayout :android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!--TODO (4) Give the TextView 16dp of padding-->
    <!--TODO (5) Set the text size to 20sp-->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello World!"
        app:layout_FrameBottom_toBottomOf="@+id/activity_main"
        app:layout_FrameLeft_toLeftOf="@+id/activity_main"
        app:layout_FrameRight_toRightOf="@+id/activity_main"
        app:layout_FrameTop_toTopOf="@+id/activity_main" />

</FrameLayout>
